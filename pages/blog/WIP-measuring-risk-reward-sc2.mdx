import { Fragment } from 'react';
import PostDetails from '../../components/PostDetails';

export const config = { unstable_runtimeJS: false };

export const postDetails = (preview = false, postPath = '') => (
    <PostDetails
        preview={preview}
        path={postPath}
        title="Measuring Risk/Reward in StarCraft 2"
        postedAt="WIP"
    />
);

<Fragment>{postDetails()}</Fragment>

Can measure risk through Monte Carlo simulations of a model.

Risk is defined as the probability the value ends up in favour of your opponent.
Simply compute P(value < 0.5) on outcomes.
