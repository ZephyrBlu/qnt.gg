import { Fragment } from 'react';
import PostDetails from '../../components/PostDetails';

export const config = { unstable_runtimeJS: false };

export const postDetails = (preview = false, postPath = '') => (
    <PostDetails
        preview={preview}
        path={postPath}
        title="Modelling Games as Time Series"
        blurb=""
        postedAt="WIP"
    />
);

<Fragment>{postDetails()}</Fragment>

- Why think about games as time series?
- Benefits of thinking about games as time series
- Downsides of games as time series
- Other approaches to analyzing games
    - Break games into periods (I.e. early, mid, late)
    - Analyze a summary of the game
