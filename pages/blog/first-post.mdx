import { Fragment } from 'react';
import PostDetails from '../../PostDetails';

export const postDetails = (preview = false, postPath = '') => (
    <PostDetails
        preview={preview}
        path={postPath}
        title="My first post"
        blurb="Some text goes here"
        postedAt="Yesterday"
        updatedAt="Today"
    />
);

<Fragment>{postDetails()}</Fragment>

### Write the post here

Hello world, how's it going?

Let's write a few paragraphs and see what it looks like with MDX.
Should look decent, I wonder what line breaks look like.
